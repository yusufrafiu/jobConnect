$(document).ready(function () {
      
      // dropdown parent active on mouseover
      // $(".dropdown-menu").hover(
      //       function() {
      //             $(this).parent().css("background-color", "#FFCC00");
      //       },
      //       function() {
      //             $(this).parent().css("background-color", "#fff");
      //       }
      // );


      // xs dropdown menu only opens when clicked
      // if ($(window).width() <= 767) {
      //       const dropdown = document.querySelectorAll('.dropdown');
      //       dropdown.forEach(dropdown => dropdown.addEventListener('click', function() {
      //             const dropdownMenu = this.querySelector('.dropdown-menu');
                  
      //             dropdownMenu.style.display = "block";
      //       }));
      // }

      // disable the above function when in xs devices
      // if ($(window).width() <= 767) {
      //       $(".dropdown-menu").hover(
      //             function () {
      //                   $(this).parent().css("background-color", "#fff");
      //             },
      //             function () {
      //                   $(this).parent().css("background-color", "#fff");
      //             }
      //       ); 
      // } 
});